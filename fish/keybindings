# Word navigation keybindings
# Only set keybindings in interactive mode
# Fish automatically handles this - bind commands are ignored in non-interactive mode
# but we add an explicit check for clarity and to suppress any potential warnings
if status is-interactive
    # Option+Left (backward-word)
    bind \e\[1\;3D backward-word
    bind \eb backward-word

    # Option+Right (forward-word)
    bind \e\[1\;3C forward-word
    bind \ef forward-word

    # Additional common sequences that might be sent
    bind \e\[1\;5D backward-word  # Ctrl+Left
    bind \e\[1\;5C forward-word   # Ctrl+Right
end
