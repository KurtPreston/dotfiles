# Force LF line endings so shell scripts run under WSL, macOS, and Linux.
# Without this, Git for Windows (core.autocrlf=true) can rewrite shebangs to
# CRLF on checkout, breaking scripts with: env: 'bash\r': No such file or directory
* text=auto eol=lf
